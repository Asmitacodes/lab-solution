// Solution 13: Join array elements into a string
function joinArray(arr) {
    console.log(`Input array: ${arr}`);
    console.log(`Joined string: ${arr.join(', ')}`);
}

joinArray(["Red", "Green", "Blue"]);
